# Examples

Drop a `.pdf`, `.docx` or `.txt` here and run:

    python cli.py examples/your-document.pdf

A scanned PDF is the most useful test case — it exercises the
selective-OCR path in `src/extract.py`.
